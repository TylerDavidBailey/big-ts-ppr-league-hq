/**
 * A public league to show someone who arrived without an ID of their own.
 *
 * Without a way in, a visitor with no league ID handy has nothing to look at
 * and leaves. Sleeper league data is public, so linking a real one costs
 * nothing, and a finished season shows every view with real content.
 *
 * Change this to point the landing page's example at a different league.
 */
export const EXAMPLE_LEAGUE_ID = '1252998165817208832';
